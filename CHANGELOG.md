# Changelog

## [Unreleased]

### Added
- Browser version (`web/`) — File System Access API, IndexedDB handle persistence
- Single-file web app with full feature parity to desktop app (except DAW launch → copy path)
